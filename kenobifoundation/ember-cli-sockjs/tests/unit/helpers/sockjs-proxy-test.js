import { sockjsProxy } from '../../../helpers/sockjs-proxy';
import { module, test } from 'qunit';

module('Unit | Helper | sockjs proxy');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sockjsProxy(42);
  assert.ok(result);
});
